VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# Only used by the standalone dev app shell (index.html / npm run dev) to
# preview the widget without embedding it — the production <booking-widget>
# custom element takes business-id as an HTML attribute instead.
VITE_DEV_BUSINESS_ID="00000000-0000-0000-0000-000000000000"
